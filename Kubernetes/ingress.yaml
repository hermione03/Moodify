apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moodifyapi-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: moodifyapi.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: moodifyapi-service
            port:
              number: 80
